# Copia este archivo a .env (misma carpeta) y completa. .env está en .gitignore: nunca se sube.
# Cadena de conexión directa: Supabase → Project Settings → Database → Connection string (URI),
# modo "Session". Reemplaza [YOUR-PASSWORD] por la contraseña de la base.
DB_URL=postgresql://postgres.ncvwgsbcvklhbyvurxzz:[YOUR-PASSWORD]@aws-0-sa-east-1.pooler.supabase.com:5432/postgres
# Supabase → Project Settings → API → service_role (secreta: ve todo, nunca en el navegador ni en el repo).
SERVICE_ROLE_KEY=
# Normalmente no hace falta cambiar estos:
SUPABASE_URL=https://ncvwgsbcvklhbyvurxzz.supabase.co
PROJECT_REF=ncvwgsbcvklhbyvurxzz
BUCKET=fotos-inventario
